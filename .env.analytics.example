# Official release builds can enable opt-in usage analytics with a dedicated GA4 web stream.
# The API secret is embedded in the built extension and must be dedicated and rotated if abused.
VITE_LSEW_GA_MEASUREMENT_ID=G-XXXXXXXXXX
VITE_LSEW_GA_API_SECRET=replace-with-a-dedicated-measurement-protocol-secret
